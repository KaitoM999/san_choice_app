name: san_choice_app
description: "A new Flutter project."
publish_to: 'none'

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  flutter_tts: ^4.0.2  
  shared_preferences: ^2.2.2  # <-- これを追加！

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/audio/
    - assets/data/
    - assets/images/  # <-- この一行を追加（フォルダごと指定できます）